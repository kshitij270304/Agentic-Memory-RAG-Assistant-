[project]
name = "mem"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aioconsole>=0.8.2",
    "chromadb>=1.0.0",
    "dspy>=3.0.3",
    "openai>=1.109.1",
]

[tool.setuptools]
packages = ["mem"]
